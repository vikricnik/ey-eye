{
  "name": "llm-pipeline-web",
  "version": "1.0.0",
  "description": "Web client for the multi-tier LLM pipeline (FastAPI + LangGraph + Ollama)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@llm-pipeline/client": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
