{
  "name": "llm-pipeline-client",
  "version": "1.0.0",
  "description": "CLI client for the multi-tier LLM pipeline (FastAPI + LangGraph + Ollama)",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@llm-pipeline/client": "^1.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
