name: duplicate-ids
version: 1

nodes:
  - id: A
    depends_on: []
    model: { provider: ollama, model: test-model }
    prompt_template: "{{ input }}"

  - id: A
    depends_on: []
    model: { provider: ollama, model: test-model }
    prompt_template: "{{ input }}"

output_node: A
