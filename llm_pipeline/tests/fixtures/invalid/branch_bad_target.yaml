name: branch-bad-target
version: 1

nodes:
  - id: classify
    depends_on: []
    model: { provider: ollama, model: test-model }
    prompt_template: "{{ input }}"

  - id: path_a
    depends_on: []
    model: { provider: ollama, model: test-model }
    prompt_template: "{{ input }}"

branches:
  - id: branch1
    from: classify
    routes:
      - default: true
        to: does_not_exist

output_node: path_a
